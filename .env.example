# Server
PORT=3000

# Better Auth session signing secret.
BETTER_AUTH_SECRET=

# At-rest source credential encryption key (HKDF-derived).
ENCRYPTION_KEY=

# Public origin where this app is reachable (used in OAuth redirects)
AUTH_URL=http://localhost:3000

# Optional comma-separated exact MCP OAuth redirect URIs beyond loopback
# http://localhost / http://127.0.0.1 callbacks.
MCP_ALLOWED_REDIRECT_URIS=

# OIDC provider (any standards-compliant issuer: Pocket ID, Authentik, Keycloak,
# Auth0, Dex, Google, Okta, …). Better Auth fetches /.well-known/openid-configuration
# from OIDC_ISSUER.
OIDC_ISSUER=https://auth.example.com
# Register a client in your provider with redirect URI:
#   ${AUTH_URL}/api/auth/oauth2/callback/oidc
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
